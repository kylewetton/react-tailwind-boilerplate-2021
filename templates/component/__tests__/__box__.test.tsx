import { mount, shallow } from 'enzyme';
import React from 'react';

import __box__ from '../__box__';

describe('__box__', () => {
  it('__box__ needs some tests', () => {
    const COMPONENT = mount(<__box__ />);
    expect(1).toEqual(2); // Fails as a reminder
  });
});
